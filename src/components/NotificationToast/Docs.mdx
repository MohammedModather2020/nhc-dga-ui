import { Meta, Story, Controls, Source } from '@storybook/blocks';
import * as Stories from './index.stories';

# toast

<Meta of={Stories} />
<Story of={Stories.Default} />
<Controls of={Stories.Default} />
<Source code={`
const clickHandler = () => {
    toast({
        rtl: false,
        duration: 5000,
        position: "top-left",
        title: "Toast title",
        description: "",
        color: "error",
        background: "white",
        actionButtons: <Button variant="outlined">ok</Button>,
    })
};

<Button onClick={clickHandler}>top-left</Button>`
        } />

